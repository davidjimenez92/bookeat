@model IEnumerable<Proyecto_net.Models.Mesa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Capacidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdlocalNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Capacidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdlocalNavigation.Idlocal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idmesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idmesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idmesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
